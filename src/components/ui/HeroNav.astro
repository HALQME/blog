---
const links = [
    { href: 'https://github.com/halqme', label: 'GitHub' },
    { href: 'https://twitter.com/halqme', label: 'Twitter' }
];
---

<nav class="flex gap-4 mt-6">
    {links.map(link => (
        <a
            href={link.href}
            target="_blank"
            rel="noopener noreferrer"
            class="text-gray-600 hover:text-gray-900 dark:text-gray-400 dark:hover:text-white transition-colors"
        >
            {link.label}
        </a>
    ))}
</nav>